{
  "name": "sails-generate-humpback-api",
  "version": "0.10.0",
  "description": "sails generate humpback-api",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "humpback-api",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "balderdashy",
  "license": "MIT",
  "dependencies": {
  	"lodash": ">=2.4.x",
  	"merge-defaults": ">=0.1.0"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "humpback-api",
    "behavior": "overrides `sails generate humpback-api`",
    "sailsVersion": "~0.10.0"
  }
}
